# Mosn Marketplace Frontend

This is a React-based frontend for the Mosn multi-vendor marketplace.

## Setup

1. Install dependencies:

```
npm install
```

2. Start the development server:

```
npm start
```

3. Open your browser at `http://localhost:3000`

## Project Structure

- `src/` - React source code
  - `components/` - Reusable UI components
  - `pages/` - Page components for routing
  - `App.js` - Main app with routing
  - `index.js` - React entry point

## Features

- Home page with banner and deals
- Product listing page (placeholder)
- Category browsing page (placeholder)

## Next Steps

- Implement product listing with filters and sorting
- Connect to backend APIs for dynamic data
- Add shopping cart and checkout flows
- Improve styling and responsiveness
